-- Обложка у истории прослушиваний и любимых треков.
--
-- В профиле оба списка рисовались серыми квадратами с нотой: интерфейс умел
-- показать обложку, но её негде было взять — в таблицах хранились только
-- название, исполнитель и uri.
--
-- Колонки nullable и без бэкфилла в SQL: обложка живёт не в нашей базе, а в
-- Spotify, и достать её миграцией неоткуда. Существующие строки дозаполняются
-- при первом чтении списка — см. infrastructure/spotify/artwork.js.

ALTER TABLE "LikedTrack" ADD COLUMN "imageUrl" TEXT;
ALTER TABLE "PlayHistory" ADD COLUMN "imageUrl" TEXT;
